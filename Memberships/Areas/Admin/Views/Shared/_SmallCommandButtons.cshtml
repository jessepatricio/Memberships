@model Memberships.Areas.Admin.Models.SmallButtonModel

<a type="button" href="@Url.Action(Model.Action)@Model.ActionParameters" class="btn @Model.ButtonType btn-sm">
    <span class="glyphicon glyphicon-@Model.Glyph"></span>
    <span class="sr-only">@Model.Text</span>
</a>
